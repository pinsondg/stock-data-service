spring:
  config:
    activate:
      on-profile: prod
  liquibase:
    default-schema: stock-data-service_prod
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://stockdatadb.cqhdyaweszhm.us-east-1.rds.amazonaws.com:5432/StockData?currentSchema=${spring.liquibase.default-schema}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}

server:
  port: 5000